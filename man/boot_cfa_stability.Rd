% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/boot_cfa_stability.R
\name{boot_cfa_stability}
\alias{boot_cfa_stability}
\title{Bootstrap CFA Stability Analysis}
\usage{
boot_cfa_stability(
  modelo,
  data,
  num_replicas,
  estimator,
  seed = NULL,
  n_cores = 2
)
}
\arguments{
\item{modelo}{Lavaan model specification.}

\item{data}{Data frame with the data.}

\item{num_replicas}{Number of bootstrap replications.}

\item{estimator}{Estimator to use (e.g., "WLSMV").}

\item{seed}{Optional random seed for reproducibility (default NULL, no seed is set).}

\item{n_cores}{Number of cores for parallel processing (default 2).}
}
\value{
Data frame with fit measures and reliability across sample sizes.
}
\description{
Performs bootstrap CFA stability analysis across different sample sizes.
}
\examples{
\donttest{
# Create sample data
set.seed(123)
n <- 300
g <- rnorm(n)
t1 <- 0.7 * g + rnorm(n, 0, 0.7)
t2 <- 0.7 * g + rnorm(n, 0, 0.7)
sim_item <- function(t) {
  as.numeric(cut(t + rnorm(length(t), 0, 0.8), c(-Inf, -1, 0, 1, Inf)))
}
data <- data.frame(
  Item1 = sim_item(t1), Item2 = sim_item(t1), Item3 = sim_item(t1),
  Item4 = sim_item(t2), Item5 = sim_item(t2), Item6 = sim_item(t2)
)

# Define CFA model
model <- "
  F1 =~ Item1 + Item2 + Item3
  F2 =~ Item4 + Item5 + Item6
"

# Run stability analysis (testing 90\% to 30\% of sample sizes)
stability_results <- boot_cfa_stability(
  modelo = model,
  data = data,
  num_replicas = 10,
  estimator = "WLSMV",
  seed = 2023,
  n_cores = 2
)

# View results - fit indices and reliability at different sample sizes
head(stability_results)

# Plot stability results
plot_cfa_stability(stability_results)
}
}
