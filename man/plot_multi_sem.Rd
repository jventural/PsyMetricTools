% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_multi_sem.R
\name{plot_multi_sem}
\alias{plot_multi_sem}
\title{Plot Multiple SEM Models}
\usage{
plot_multi_sem(
  models,
  titles = NULL,
  layout = "tree2",
  rotation = 2,
  whatLabels = "std",
  residuals = FALSE,
  intercepts = FALSE,
  thresholds = FALSE,
  sizeMan = 10,
  sizeMan2 = 5,
  sizeLat = 12,
  label.cex = 1,
  edge.label.cex = 2,
  edge.color = "grey40",
  color = list(lat = "grey80", man = "grey90"),
  mar = c(4, 4, 4, 4),
  outerMar = c(1, 1, 3, 1),
  show_fit_indices = TRUE,
  fit_indices = c("cfi", "tli", "rmsea", "srmr"),
  use_scaled = FALSE,
  model_descriptions = NULL,
  custom_titles = NULL,
  title.cex = 0.8,
  title.font = 2,
  save_plot = FALSE,
  filename = "sem_models_plot",
  file_format = "png",
  width_per = 4,
  height = 4,
  dpi = 300,
  units = "in"
)
}
\arguments{
\item{models}{List of lavaan model objects.}

\item{titles}{Titles for each model (default NULL).}

\item{layout}{Layout type (default "tree2").}

\item{rotation}{Rotation value (default 2).}

\item{whatLabels}{Labels to show (default "std").}

\item{residuals}{Show residuals (default FALSE).}

\item{intercepts}{Show intercepts (default FALSE).}

\item{thresholds}{Show thresholds (default FALSE).}

\item{sizeMan}{Manifest variable size (default 10).}

\item{sizeMan2}{Second manifest size (default 5).}

\item{sizeLat}{Latent variable size (default 12).}

\item{label.cex}{Label size multiplier (default 1).}

\item{edge.label.cex}{Edge label size (default 2).}

\item{edge.color}{Edge color (default "grey40").}

\item{color}{Color list for nodes.}

\item{mar}{Inner margins.}

\item{outerMar}{Outer margins.}

\item{show_fit_indices}{Show fit indices (default TRUE).}

\item{fit_indices}{Which fit indices to show.}

\item{use_scaled}{Use scaled indices (default FALSE).}

\item{model_descriptions}{Model descriptions.}

\item{custom_titles}{Custom titles.}

\item{title.cex}{Title size (default 0.8).}

\item{title.font}{Title font (default 2).}

\item{save_plot}{Save plot to file (default FALSE).}

\item{filename}{Output filename.}

\item{file_format}{Output format (png, pdf, tiff, jpeg).}

\item{width_per}{Width per plot in inches.}

\item{height}{Height in inches.}

\item{dpi}{Resolution.}

\item{units}{Units for dimensions.}
}
\value{
NULL (plots are drawn to device).
}
\description{
Creates a panel of SEM path diagrams for multiple models.
}
\examples{
\dontrun{
library(lavaan)

# Create sample data
set.seed(123)
n <- 300
data <- data.frame(
  Item1 = sample(1:5, n, replace = TRUE),
  Item2 = sample(1:5, n, replace = TRUE),
  Item3 = sample(1:5, n, replace = TRUE),
  Item4 = sample(1:5, n, replace = TRUE),
  Item5 = sample(1:5, n, replace = TRUE),
  Item6 = sample(1:5, n, replace = TRUE)
)

# Fit multiple models
model1 <- "F1 =~ Item1 + Item2 + Item3 + Item4 + Item5 + Item6"
model2 <- "F1 =~ Item1 + Item2 + Item3\nF2 =~ Item4 + Item5 + Item6"

fit1 <- cfa(model1, data = data, ordered = TRUE, estimator = "WLSMV")
fit2 <- cfa(model2, data = data, ordered = TRUE, estimator = "WLSMV")

# Plot multiple models side by side
plot_multi_sem(
  models = list(fit1, fit2),
  model_descriptions = c("1-Factor Model", "2-Factor Model"),
  show_fit_indices = TRUE,
  fit_indices = c("cfi", "tli", "rmsea", "srmr"),
  use_scaled = TRUE
)

# Save to file
plot_multi_sem(
  models = list(fit1, fit2),
  model_descriptions = c("Model 1", "Model 2"),
  save_plot = TRUE,
  filename = "sem_comparison",
  file_format = "png",
  width_per = 5,
  height = 5,
  dpi = 300
)
}
}
