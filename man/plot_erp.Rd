% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/erp_analysis.R
\name{plot_erp}
\alias{plot_erp}
\title{Plot Extreme-Response Proportion}
\usage{
plot_erp(erp_table, threshold = 0.3)
}
\arguments{
\item{erp_table}{A data frame produced by \code{\link{erp_by_item}}.}

\item{threshold}{Numeric threshold for the reference line (default 0.30).}
}
\value{
A \code{ggplot} object.
}
\description{
Creates a horizontal bar chart of ERP values per item, with a
  dashed reference line at the chosen threshold. Items exceeding the threshold
  are visually distinguished by fill colour.
}
\examples{
\dontrun{
set.seed(123)
df <- data.frame(
  IT1 = sample(1:5, 200, replace = TRUE),
  IT2 = sample(1:5, 200, replace = TRUE),
  IT3 = sample(1:5, 200, replace = TRUE)
)
erp <- erp_by_item(df, c("IT1", "IT2", "IT3"))
plot_erp(erp)
}
}
