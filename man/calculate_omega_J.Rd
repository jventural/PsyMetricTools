% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calculate_omega_J.R
\name{calculate_omega_J}
\alias{calculate_omega_J}
\title{Omega Coefficient from a Fitted lavaan Model}
\usage{
calculate_omega_J(items, fit, digits = 3)
}
\arguments{
\item{items}{A character vector of item names \emph{or} a named list of
character vectors (one element per factor).}

\item{fit}{A fitted lavaan object (e.g., from \code{\link[lavaan]{cfa}} or
an EFA specification produced by \code{EFA_modern}).}

\item{digits}{Integer; number of decimal places for rounding (default 3).}
}
\value{
When \code{items} is a single vector, a numeric scalar (omega).
  When \code{items} is a named list, a named numeric vector with one omega
  per factor.
}
\description{
Computes McDonald's omega using the R-squared values
  (communalities) extracted directly from a fitted lavaan object. Accepts
  either a single character vector of item names or a named list of vectors
  to compute omega for multiple factors at once.
}
\examples{
\dontrun{
library(lavaan)
model <- 'F1 =~ x1 + x2 + x3
           F2 =~ x4 + x5 + x6'
fit <- cfa(model, data = HolzingerSwineford1939)

# Single factor
calculate_omega_J(c("x1", "x2", "x3"), fit)

# Multiple factors at once
calculate_omega_J(
  list(F1 = c("x1", "x2", "x3"),
       F2 = c("x4", "x5", "x6")),
  fit
)
}
}
