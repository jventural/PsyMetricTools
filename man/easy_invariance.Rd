% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/easy_invariance.R
\name{easy_invariance}
\alias{easy_invariance}
\title{Analisis de Invarianza Factorial Simplificado}
\usage{
easy_invariance(
  model,
  data,
  estimator,
  ordered,
  ID.cat,
  group,
  levels_of_invariance,
  group.partial = NULL,
  fit_indices = c("CFI", "TLI", "RMSEA", "SRMR")
)
}
\arguments{
\item{model}{Modelo de lavaan en formato texto.}

\item{data}{Data frame con los datos.}

\item{estimator}{Estimador a utilizar (por defecto "WLSMV").}

\item{ordered}{Logical indicando si las variables son ordinales.}

\item{ID.cat}{Metodo de identificacion para variables categoricas (e.g., "Wu.Estabrook.2016").}

\item{group}{Nombre de la variable de agrupacion.}

\item{levels_of_invariance}{Vector con los niveles de invarianza a evaluar:
"configural", "threshold", "metric", "strict".}

\item{group.partial}{Vector opcional de parametros a liberar parcialmente.}

\item{fit_indices}{Vector con los indices de ajuste a incluir. Opciones disponibles:
"CFI", "TLI", "RMSEA", "SRMR", "CRMR". Por defecto incluye c("CFI", "TLI", "RMSEA", "SRMR").
Para excluir RMSEA (util con pocos grados de libertad), usar c("CFI", "TLI", "SRMR", "CRMR").}
}
\value{
Lista con combined_data (tabla resumen) y los modelos ajustados.
}
\description{
Realiza analisis de invarianza factorial para datos ordinales
  utilizando el enfoque de Wu y Estabrook (2016) con estimador WLSMV.
}
\examples{
\dontrun{
# Ejemplo con todos los indices por defecto
res <- easy_invariance(
  model = "F =~ item1 + item2 + item3",
  data = mydata,
  estimator = "WLSMV",
  ordered = TRUE,
  ID.cat = "Wu.Estabrook.2016",
  group = "sex",
  levels_of_invariance = c("configural", "threshold", "metric", "strict")
)

# Ejemplo excluyendo RMSEA
res <- easy_invariance(
  model = "F =~ item1 + item2 + item3",
  data = mydata,
  estimator = "WLSMV",
  ordered = TRUE,
  ID.cat = "Wu.Estabrook.2016",
  group = "sex",
  levels_of_invariance = c("configural", "threshold", "metric", "strict"),
  fit_indices = c("CFI", "TLI", "SRMR", "CRMR")
)
}
}
