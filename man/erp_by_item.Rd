% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/erp_analysis.R
\name{erp_by_item}
\alias{erp_by_item}
\title{Extreme-Response Proportion by Item}
\usage{
erp_by_item(data, items, extremes = c(1, 5), threshold = 0.3)
}
\arguments{
\item{data}{A data frame containing the item responses.}

\item{items}{Character vector of column names to analyse.}

\item{extremes}{Numeric vector of length 2 indicating the lowest and highest
anchor values (default \code{c(1, 5)}).}

\item{threshold}{Numeric threshold above which an item is flagged
(default 0.30).}
}
\value{
A data frame with one row per item containing: \code{Item}, \code{n},
  \code{p_low}, \code{p_high}, \code{ERP}, \code{Median}, \code{IQR},
  \code{Flag_ERP}, and formatted percentage columns. Rows are sorted by
  descending ERP.
}
\description{
Computes the Extreme-Response Proportion (ERP) for each item in
  a Likert-type scale. ERP is the proportion of respondents who selected
  either the lowest or highest response option, which can signal floor/ceiling
  effects or poor item discrimination.
}
\examples{
\dontrun{
set.seed(123)
df <- data.frame(
  IT1 = sample(1:5, 200, replace = TRUE),
  IT2 = sample(1:5, 200, replace = TRUE),
  IT3 = sample(1:5, 200, replace = TRUE)
)
erp <- erp_by_item(df, c("IT1", "IT2", "IT3"))
print(erp)
}
}
