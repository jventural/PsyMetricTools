% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/boot_cfa_raincloud.R
\name{boot_cfa_raincloud}
\alias{boot_cfa_raincloud}
\title{Raincloud Plot para Bootstrap CFA}
\usage{
boot_cfa_raincloud(
  df,
  save = TRUE,
  path = "Plot_boot_raincloud.jpg",
  dpi = 600,
  exclude_indices = NULL,
  color_scheme = "ocean",
  show_stats = TRUE,
  theme_style = "modern",
  ...
)
}
\arguments{
\item{df}{Data frame con resultados de boot_cfa().}

\item{save}{Logical. Guardar el grafico (default TRUE).}

\item{path}{Ruta para guardar el grafico (default "Plot_boot_raincloud.jpg").}

\item{dpi}{Resolucion en DPI (default 600).}

\item{exclude_indices}{Vector de indices a excluir (e.g., c("RMSEA")).}

\item{color_scheme}{Esquema de color: "ocean", "sunset", "forest", "lavender", "monochrome", "elegant".}

\item{show_stats}{Mostrar estadisticos en el grafico (default TRUE).}

\item{theme_style}{Estilo: "modern", "minimal", "dark" (default "modern").}

\item{...}{Argumentos adicionales para ggsave.}
}
\value{
Un objeto ggplot.
}
\description{
Crea visualizaciones raincloud elegantes para resultados de bootstrap CFA.
}
\examples{
\dontrun{
# First run boot_cfa to get bootstrap results
set.seed(123)
n <- 300
data <- data.frame(
  Item1 = sample(1:5, n, replace = TRUE),
  Item2 = sample(1:5, n, replace = TRUE),
  Item3 = sample(1:5, n, replace = TRUE),
  Item4 = sample(1:5, n, replace = TRUE),
  Item5 = sample(1:5, n, replace = TRUE),
  Item6 = sample(1:5, n, replace = TRUE)
)

model <- "
  F1 =~ Item1 + Item2 + Item3
  F2 =~ Item4 + Item5 + Item6
"

boot_results <- boot_cfa(
  new_df = data,
  model_string = model,
  item_prefix = "Item",
  n_replications = 100
)

# Create raincloud plot with ocean color scheme
boot_cfa_raincloud(boot_results,
                   save = TRUE,
                   path = "bootstrap_raincloud.jpg",
                   color_scheme = "ocean",
                   show_stats = TRUE,
                   theme_style = "modern")

# Dark theme with sunset colors
boot_cfa_raincloud(boot_results,
                   save = FALSE,
                   color_scheme = "sunset",
                   theme_style = "dark")

# Available color schemes: "ocean", "sunset", "forest", "lavender", "monochrome", "elegant"
}
}
